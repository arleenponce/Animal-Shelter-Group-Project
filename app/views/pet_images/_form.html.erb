<%= form_for(@pet_image) do |f| %>
  <% if @pet_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pet_image.errors.count, "error") %> prohibited this pet_image from being saved:</h2>

      <ul>
      <% @pet_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pet_id %><br>
    <%= f.number_field :pet_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
